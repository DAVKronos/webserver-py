services:
  python:
    command: python -Xfrozen_modules=off -m debugpy --wait-for-client --listen 0.0.0.0:5678 -m app.main
    ports:
      - 5678:5678
  frontend:
    command: node esbuild.js --watch
